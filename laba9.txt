#include <iostream> 
#include <iomanip> 
#include "windows.h"

using namespace std;

int main()
{
    int x, k, n = 5;
    cout << "Enter x: ";
    cin >> x;
    cout << "Enter k: ";
    cin >> k;
    float rez1;
    int sum = 0;
    for (int i = 1; i <= 5; i++)
    {
        sum = sum + i;
    }
    rez1 = tan(sum = 2 * x);
    float rez2;
    int pr = 1;
    for (int i = 1; i <= 10; i++);
    {
        pr = pr + (log(k - rez1));
        rez2 = pr + (rez1 * rez1);
    }
    
    cout << "y=" << rez1 << endl;
    cout << "z=" << rez2 << endl;
    system("pause");
    return 0;
}
